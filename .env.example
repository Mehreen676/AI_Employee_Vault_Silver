# =============================================================
# AI Employee Vault — Silver Tier  |  Environment Variables
# =============================================================
# Copy this file to .env (gitignored) and fill in your values.
# Never commit .env to version control.
# In GitHub Actions, add these as Repository Secrets.
# =============================================================

# ---- OpenAI (optional — agent uses fallback if missing) ----
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ---- LinkedIn (optional — simulated mode if missing) --------
# Get token: https://developer.linkedin.com/
# Example URN: urn:li:person:AbCdEfGhIjKl
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_PERSON_URN=
# Set to "true" to force simulated mode even if token is present
LINKEDIN_SIMULATED=true

# ---- Gmail OAuth guard (cloud safety) -----------------------
# Set to "true" ONLY if OAuth credentials are available in cloud.
# Default is "false" — gmail_watcher skips cleanly in cloud.
GMAIL_OAUTH_ENABLED=false

# ---- Email (SMTP) — optional, BONUS feature -----------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ---- Calendar — optional, BONUS feature ---------------------
# (currently simulated locally; add Google Calendar creds here)
GOOGLE_CALENDAR_ID=
